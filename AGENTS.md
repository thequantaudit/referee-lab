## Learned User Preferences
- Keep monwell57 as the default long-term GitHub account; use thequantaudit for AI-related work and repos

## Learned Workspace Facts
- This repo is `thequantaudit/referee-lab`; origin uses `git@github.com-ai:thequantaudit/referee-lab.git` (SSH host alias), not plain `github.com` or HTTPS under monwell57
- Dual GitHub SSH on this machine: monwell57 via `github.com` + `~/.ssh/id_ed25519`; thequantaudit via `github.com-ai` + `~/.ssh/id_ed25519_ai`
