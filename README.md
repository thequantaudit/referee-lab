# referee-lab

`methodology/` — the standard, versioned  
`docket/` — candidate models and selection notes  
`teardowns/` — one folder per model, numbered  
`harness/` — the fixed evaluation code every teardown runs  
`scoreboard/` — the permanent, public record of verdicts
